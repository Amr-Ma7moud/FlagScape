# 🏗️ FlagScape Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    USER'S BROWSER                           │
│                   (localhost:8080)                          │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ HTTP Requests
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                  FRONTEND (Static Files)                    │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  HTML Pages                                          │   │
│  │  • index.html (World Map)                            │   │
│  │  • guessTheFlag.html                                 │   │
│  │  • capitalChallenge.html                             │   │
│  │  • speedQuiz.html                                    │   │
│  │  • countryDetails.html                               │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  JavaScript Modules                                  │   │
│  │  • index.js                                          │   │
│  │  • guessTheFlag.js ────┐                            │   │
│  │  • capitalChallenge.js ├──► All make API calls      │   │
│  │  • speedQuiz.js        │    to localhost:5000       │   │
│  │  • guessFlagGame.js    │                            │   │
│  │  • countryModal.js ────┘                            │   │
│  │  • countryDetails.js                                │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ Fetch API Calls
                            │ http://localhost:5000/v3.1/*
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              BACKEND (Flask Server)                         │
│                   (localhost:5000)                          │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Flask Application (app.py)                          │   │
│  │                                                       │   │
│  │  API Endpoints:                                      │   │
│  │  ┌────────────────────────────────────────────────┐  │   │
│  │  │ GET /v3.1/all                                  │  │   │
│  │  │ • Returns all countries                        │  │   │
│  │  │ • Supports ?fields=name,capital,etc filtering │  │   │
│  │  └────────────────────────────────────────────────┘  │   │
│  │  ┌────────────────────────────────────────────────┐  │   │
│  │  │ GET /v3.1/name/<country_name>                 │  │   │
│  │  │ • Search by country name                       │  │   │
│  │  │ • Supports ?fullText=true for exact match     │  │   │
│  │  └────────────────────────────────────────────────┘  │   │
│  │                                                       │   │
│  │  Flask-CORS: Enabled for all origins                │   │
│  └──────────────────────────────────────────────────────┘   │
│                            │                                │
│                            ▼                                │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Local Data Store                                    │   │
│  │  backend/data/countries.json                         │   │
│  │  • 250+ countries                                    │   │
│  │  • Seeded from REST Countries API                   │   │
│  │  • Complete country information                     │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ▲
                            │ Initial Seeding Only
                            │ (One-time operation)
┌─────────────────────────────────────────────────────────────┐
│          External REST Countries API                        │
│         https://restcountries.com/v3.1/                    │
│  • Used only during initial data seeding                   │
│  • Data stored locally for offline use                     │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Request Flow Example

### Scenario: User Opens a Country Modal

```
1. User clicks on "Egypt" on the world map
   │
   ├─► countryModal.js calls:
   │   fetch('http://localhost:5000/v3.1/name/egypt?fullText=true')
   │
2. Flask server receives request
   │
   ├─► app.py processes the request
   │   • Loads data from countries.json
   │   • Filters for "Egypt" (case-insensitive)
   │   • Returns matching country data
   │
3. Response sent back as JSON
   │
   ├─► countryModal.js receives data
   │   • Populates modal with country info
   │   • Displays flag, capital, population, etc.
   │
4. User sees the country information modal
```

## 📊 Data Flow

```
[Initial Setup]
REST Countries API ──► seed.py ──► countries.json (250 countries)

[Runtime]
Browser ──► Frontend JS ──► Flask API ──► countries.json ──► Response
```

## 🎯 Key Integration Points

1. **API Compatibility**: Flask endpoints mimic REST Countries API structure
2. **CORS Handling**: Flask-CORS allows frontend-backend communication
3. **Field Filtering**: Backend supports selective field queries for performance
4. **Search Functionality**: Name-based search with partial/full text matching
5. **Local Storage**: All data cached locally for offline operation

## 💡 Benefits

- **Speed**: Local API calls are faster than external requests
- **Reliability**: No dependency on external service uptime
- **Development**: Easy debugging and customization
- **Privacy**: Data stays on your machine
- **Cost**: No API rate limits or usage fees
